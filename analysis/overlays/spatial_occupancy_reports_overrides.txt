#
# Spatial occupancy + node position reports (optional overlay).
# Apply AFTER routing_contact_reports_overrides.txt so report1–7 stay unchanged
# and only nrofReports + report8/report9 are extended:
#
#   python3 scenarios/analysis/run_all_scenarios.py --corpus corpus_v1 \
#     --extra-settings scenarios/analysis/overlays/routing_contact_reports_overrides.txt \
#     --extra-settings scenarios/analysis/overlays/spatial_occupancy_reports_overrides.txt
#
# Settings for each report use the class namespace (NOT Report.report8.*):
#   NodePositionReport.positionSampleInterval
#   NodePositionReport.output
#   NodePositionReport.nodes          (optional, CSV of host addresses)
#   SpatialOccupancyReport.gridSize
#   SpatialOccupancyReport.timeBinSize
#   SpatialOccupancyReport.sampleInterval
#

Report.nrofReports = 9
Report.report8 = NodePositionReport
Report.report9 = SpatialOccupancyReport

# CSV node positions (explicit .csv path)
NodePositionReport.output = reports/%%Scenario.name%%_NodePositionReport.csv
NodePositionReport.positionSampleInterval = 60

SpatialOccupancyReport.gridSize = 50
SpatialOccupancyReport.timeBinSize = 300
SpatialOccupancyReport.sampleInterval = 60
