# map_space_v1 (cleaned)

This directory was cleaned in `map_generation_cleanup_phase1`.

- Previous `map_space_v1` generated outputs were removed.
- Legacy methodology artifacts were archived outside this working directory.
- This folder is intentionally kept minimal while the new batch-based pipeline is implemented.

Do not use old `map_space_v1` reports as active methodology.
