# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
*.so
.venv/
venv/
env/
internal/

# Streamlit (secretos locales)
.streamlit/secrets.toml

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log

# Clone of GitHub Wiki (solo para publicar; el contenido editable está en wiki/)
.wiki-clone/

# Backups locales (tar.gz grandes; no subir a GitHub)
# Nota: .gitignore solo evita ficheros NUEVOS; si ya estaban en git hay que quitarlos con git rm --cached
analysis/backups/
_archive/

# Map pipeline — datos regenerables (bootstrap_maps.sh los recrea)
maps/raw/
maps/processed/
setup/.venv_osmnx/
